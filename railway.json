{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.backend"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "restartPolicy": {
      "policy": "ON_FAILURE"
    }
  }
}
